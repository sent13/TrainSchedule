<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Light設定時に適用されるテーマ(デフォルトのテーマ) -->
    <style name="NoActionBarThemeLight" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="fukidasiRef">@drawable/fukidasi1</item>
        <item name="adapterTextColor">@color/black</item>
        <item name="charaMakeEditorRef">@drawable/frame1</item>
    </style>

    <!-- Dark設定時に適用されるテーマ -->
    <style name="NoActionBarThemeDark" parent="Theme.AppCompat.NoActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="fukidasiRef">@drawable/fukidasi2</item>
        <item name="adapterTextColor">@color/white</item>
        <item name="charaMakeEditorRef">@drawable/frame2</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />




</resources>
